# Rojin Pourkhomami — Product Designer Portfolio (Animated)

A playful, animated **single-file portfolio website** for **Rojin Pourkhomami** built with pure **HTML + CSS + JavaScript** (no frameworks, no build step).  
It features floating shapes, soft gradients, scroll-reveal, tilt cards, and “magnetic” buttons to keep the UI fun and interactive.

## ✨ Features
- **Single file** setup (`index.html`) — easy to deploy anywhere
- **Animated background** (gradient drift + subtle grain texture)
- **Floating SVG blobs** for depth and motion
- **Scroll reveal** animations as you move through sections
- **Hover tilt** effect on project cards
- **Magnetic buttons** (buttons subtly follow the cursor)
- **Cursor aura** glow that tracks movement
- **Contact section** with:
  - Demo form behavior (no backend required)
  - **Copy-to-clipboard email** button

## 🧱 Built With
- HTML5
- CSS3 (gradients, blur, animations)
- Vanilla JavaScript (IntersectionObserver + hover interactions)

No external dependencies.

---

## 🚀 Getting Started

### Option 1: Run with VS Code Live Server
1. Open the folder in VS Code
2. Install the **Live Server** extension (by Ritwick Dey)
3. Right-click `index.html` → **Open with Live Server**
4. Your portfolio opens in the browser (usually at `http://127.0.0.1:5500`)

### Option 2: Open directly in a browser
Just double-click `index.html` and it will run locally.

---

## 🗂️ Project Structure
```txt
/
└── index.html
